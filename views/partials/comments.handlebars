<div class="comments" >
  {{#each this}}
  <section class="comment mt-3 p-2 rounded" id="comments-div"> 
    <div>
      {{user.username}} on {{format_date created_at}}
    </div>
    <div>
      {{comment_text}}
    </div>
  </section>
  {{/each}}
</div>